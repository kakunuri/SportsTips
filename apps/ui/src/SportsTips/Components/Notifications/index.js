import { toast } from 'react-toastify';
export function notifySuccess(info){
    toast(info);
}
export function notifyFailure(info){
    toast(info);
}
export function notifyInfo(info){
    toast(info);
}